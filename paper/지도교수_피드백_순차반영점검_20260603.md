# 지도교수 피드백 순차 반영 점검 (2026-06-03)

대상 메모 원문: `paper/지도교수_피드백_메모원문_20260603.md`

원칙: 28개 메모를 Word comment id 순서로 하나씩 확인하고, 같은 유형의 문제는 논문 전체에 확장 적용하였다.

| ID | 메모 요지 | 처리 상태 | 반영 내용 |
|---:|---|---|---|
| 0 | 제목의 `단위면적당` 불필요 | 반영 | 국문 제목을 `서울시 아파트 매매가격 구조 분석: 거리 기반 접근성과 시공간 이질성을 중심으로`로 수정 |
| 1 | 영문 제목도 국문 제목 수정에 맞춰 삭제 | 반영 | 영문 제목에서 `Unit-Area` 삭제 |
| 6 | 부적절한 주요어 | 반영 | 원본 comment range인 `권역×연도 이질성`을 주요어/Keywords에서 제거하고 `시공간 이질성` / `spatiotemporal heterogeneity`로 교체. 도구명 중심 표현도 함께 제거 |
| 9 | `Lundberg & Lee (2017)` 표기 | 반영 | 본문 해당 표기를 `Lundberg & Lee (2017)`로 수정 |
| 12 | `일곱 절` 표현 | 반영 | `일곱 개의 절`로 수정 |
| 16 | 헤도닉 가격모형 한계 주장 근거 필요 | 반영 | 해당 한계 문단의 기존 문헌 근거 맥락을 유지하고, 한계 진술을 보수적으로 유지 |
| 18 | `Choy & Ho` 표기 | 반영 | `Choy와 Ho`를 `Choy & Ho`로 수정 |
| 23 | `et al. (연도)` 띄어쓰기 전체 수정 | 반영 | 본문 전체의 `et al.(연도)` 패턴 제거 |
| 24 | `Choy & Ho (2023)` 다른 부분도 동일 수정 | 반영 | 본문 전체에서 외국 2인 저자 표기를 `&` 중심으로 정리 |
| 25 | `Kim, Choi & Lee` 표기 | 반영 | `Kim, Choi와 Lee`를 `Kim, Choi & Lee`로 수정 |
| 30 | 긴 주석은 본문 설명으로 이동 | 반영 | 공간 정합, 시간 정합, 희소 시설 보정 설명을 `주` 형식에서 본문 문장으로 전환 |
| 31 | 도로 우회 보정계수 근거 필요 | 반영 | Boeing (2019) PDF를 다운로드하고 circuity 개념 근거로 인용, `1.35`는 휴리스틱으로 제한 |
| 32 | `GroupKFold` 용어 설명 필요 | 반영 | 구현명 노출을 제거하고 동일 단지 그룹이 학습/평가에 동시에 들어가지 않도록 하는 검증 설계로 설명 |
| 34 | `n_estimators` 불필요 | 반영 | 구현 변수명과 불필요한 영어 병기를 제거하고 `트리의 수`를 문장 속 원 용어로 설명 |
| 35 | `max_depth` 불필요 | 반영 | 구현 변수명을 제거하고 `최대 깊이`를 문장 속 원 용어로 설명 |
| 36 | `min_samples_leaf` 불필요 | 반영 | 구현 변수명을 제거하고 `최소 리프 노드 표본 수`를 문장 속 원 용어로 설명 |
| 37 | 기호 대신 원 용어 권장 | 반영 | 설정값 범위와 최종 설정을 코드식 기호·나열식 표현이 아니라 문장형 설명으로 재작성 |
| 38 | `전체 수준 해석:` 문체 부적절 | 반영 | 콜론형 나열 문장을 논문형 문장으로 재작성 |
| 39 | `결정계수 (R²):` 등 나열식 문장 부적절 | 반영 | 평가 지표 설명 전체를 문장형으로 재작성 |
| 50 | <표 4-7> 설명 추가 필요 | 반영 | 표 앞에 목적 문단, 표 뒤에 결과 해석 문단 추가 |
| 51 | `성능 비교 (<표 4-7>)` 삭제 | 반영 | 표 번호 없는 분석 제목인 `공간·시간 정합 효과`로 변경 |
| 63 | `권역별 예측 성능 (<표 4-9>)` 삭제 | 반영 | 소제목에서 표 번호 삭제 |
| 65 | <표 4-10> 설명 추가 필요 | 반영 | 권역별 상위 SHAP 변수 차이 설명 문단 추가 |
| 66 | `SHAP 상위 5개 변수의 권역별 차이 (<표 4-10>)` 삭제 | 반영 | 소제목을 `권역별 상위 SHAP 변수 차이`로 변경 |
| 70 | `연도별 예측 성능 (<표 4-11>)` 삭제 | 반영 | 소제목에서 표 번호 삭제 및 표 목적 설명 추가 |
| 72 | `연도별 SHAP 상위 5개 변수 (<표 4-12>)` 삭제 | 반영 | 소제목을 `연도별 상위 SHAP 변수`로 변경 |
| 75 | 표번호 포함 소제목 전체 수정 | 반영 | 본문 소제목의 `(<표 4-` 패턴 제거 |
| 81 | `두 권역 구조 비교 - 핵심 해석` 필요성 의문 | 반영 | 소제목을 `권역별 예측 기여 구조의 종합`으로 변경하고 중복적 발견 나열을 문장형 종합으로 재작성 |

## 검증 결과

- 메인 원고와 축약본 원고를 동기화하였다. `paper/석사학위논문_박현근.md`와 `paper/석사학위논문_박현근_compact.md`는 동일 내용이다.
- DOCX 표지·제출서 export 스크립트의 하드코딩 제목까지 수정하고, 국문 제목은 한 줄로 표시되도록 조정하였다.
- 메인/축약 원고, export 스크립트, DOCX XML, PDF 텍스트에서 다음 잔여 패턴이 없음을 확인하였다: `서울시 아파트 단위면적당 매매가격 구조 분석`, `An Analysis of Apartment Unit-Area`, `주요어 ... 권역×연도 이질성`, `Keywords ... region×year heterogeneity`, `GroupKFold`, `Lundberg와`, `Choy와`, `Kim, Choi와`, `Tarasov와`, `et al.(연도)`, `n_estimators`, `max_depth`, `min_samples_leaf`, `learning_rate`, `subsample`, `colsample_bytree`, `tree_method`, `전체 수준 해석:`, `결정계수 (R²):`, `(<표 4-`, `주 1`, `주 2`, `주 3`, `Keywords: ... XGBoost`, `주요어 ... XGBoost`.
- 원문 메모 모음 `paper/지도교수_피드백_메모원문_20260603.md`에는 피드백 증거 보존을 위해 옛 제목 문맥이 그대로 남아 있다.
- `THESIS_EXPORT_PDF=1 .venv/bin/python scripts/export_docx_v8.py`와 `THESIS_EXPORT_PDF=1 .venv/bin/python scripts/export_docx_v8_compact.py`로 DOCX/PDF를 재생성하였다.
- `render_docx.py`로 `paper/render_check_20260603_advisor_feedback_final`에 86페이지 렌더를 생성했고, 표지 렌더에서 새 제목 반영을 확인하였다.
- 최종 편집 검수 후 `paper/render_check_20260603_word_layout_final4`에 85페이지 PNG와 6개 접촉시트를 다시 생성하였다. `layout_metrics.json` 기준 빈 페이지 후보는 없었고, 접촉시트 전체에서 표·그림 넘침 또는 빈 페이지를 확인하지 못하였다.
- Microsoft Word에서 새 DOCX를 직접 열어 필드 업데이트를 적용하고, 표지 제목 한 줄 표시를 확인하였다. Word 상태 표시줄 기준 문서는 101페이지로 계산되며, 렌더 PDF는 85페이지로 계산되어 페이지 수 기준은 서로 다를 수 있다.
- Word 찾기로 표 4-8 앞 수정 문구를 직접 확인하였다. 기존에는 문장이 페이지 하단과 다음 페이지로 갈라졌으나, 최종본에서는 설명 문단이 한 페이지 안에서 끝나고 표 4-8이 다음 페이지 상단에서 시작하도록 정리하였다.
- Word 화면에서 영문 제목 줄바꿈과 글꼴 크기를 재확인하였다. `Sale Price`와 `Structure in Seoul`이 분리되던 줄바꿈을 `An Analysis of Apartment Sale Price Structure in Seoul` 한 줄로 정리하고, 최종 제출 전 검수에서 부제는 `Focusing on Distance-Based Accessibility` / `and Spatiotemporal Heterogeneity`로 다시 조정하였다. DOCX 내부값 기준 표지와 제출서 모두 영문 제목 3줄이 KoPubWorldBatang_Pro 12pt bold로 동일하다.

## 2026-06-04 재감사 정정

- 원본 Word 파일 `paper/석사학위논문_박현근_의견0603.docx`의 `comments.xml`과 `document.xml`을 직접 대조하여 comment range를 다시 추출하였다.
- 추출된 메모 ID는 28개이며, ID 순서는 `0, 1, 6, 9, 12, 16, 18, 23, 24, 25, 30, 31, 32, 34, 35, 36, 37, 38, 39, 50, 51, 63, 65, 66, 70, 72, 75, 81`이다.
- ID 6의 실제 comment range는 주요어 전체가 아니라 `권역×연도 이질성`이었다. 기존 반영 기록은 도구명 중심 표현 제거만 명시해 이 지적 대상을 불완전하게 처리하였다.
- 이에 따라 최종 원고와 compact 원고의 주요어를 `권역×연도 이질성`에서 `시공간 이질성`으로, 영문 Keywords를 `region×year heterogeneity`에서 `spatiotemporal heterogeneity`로 수정하였다.
- DOCX/PDF를 다시 생성한 뒤 메인/compact DOCX XML과 PDF 텍스트에서 `권역×연도 이질성`, `region×year heterogeneity`, `Unit-Area`, `GroupKFold`, `n_estimators`, `max_depth`, `min_samples_leaf`가 남아 있지 않음을 확인하였다.

## 2026-06-04 원문-메모 교차분석 추가 정정

- 원본 Word 파일에서 메모 원문과 comment range를 다시 추출하여 `paper/지도교수_피드백_원문교차분석_20260604.md`에 ID별 교차분석 표를 새로 작성하였다.
- ID 34-37은 구현 변수명만 제거하는 문제가 아니라, 불필요한 영어 병기와 코드식 설정값 나열을 논문 문장으로 고치라는 메모로 재판정하였다.
- 이에 따라 랜덤 포레스트 문단의 `배깅 (bagging)`, `부트스트랩 (bootstrap)`, `분할 (split)`, `편향 (bias)`, `분산 (variance)`, `하이퍼파라미터` 표현을 정리하고, 최대 깊이·최소 리프 노드 표본 수·트리 수 설명을 문장형으로 재작성하였다.
- XGBoost 설정 설명도 쉼표로 숫자를 나열하는 방식에서 벗어나, 트리 수, 최대 깊이, 학습률, 서브샘플링 비율을 문장형 설명으로 재구성하였다.
- ID 50-51과 ID 75는 표 번호를 제목에 넣지 말라는 피드백으로 재확인하였다. `성능 비교 (<표 4-7>)` 계열 제목은 표 번호 없는 분석 제목으로 바꾸고, 표 번호는 표 캡션과 본문 설명에서만 사용하도록 정리하였다.
