# 지도교수 피드백 수정기록 (2026-06-03)

대상 피드백: `석사학위논문_박현근_의견0603.docx`

수정 원칙: 메모가 직접 표시한 문장뿐 아니라 같은 유형의 문제가 본문 다른 곳에 반복되면 동일 기준으로 확장 수정하였다. 새로 보강한 문헌은 다운로드 가능한 원문 PDF를 확보한 자료만 사용하였다.

## 반영 내역

1. 제목 및 주요어
   - 국문 제목에서 `단위면적당`을 삭제하여 `서울시 아파트 매매가격 구조 분석: 거리 기반 접근성과 시공간 이질성을 중심으로`로 수정하였다.
   - 영문 제목에서 대응 표현인 `Unit-Area`를 삭제하여 `An Analysis of Apartment Sale Price Structure in Seoul: Focusing on Distance-Based Accessibility and Spatiotemporal Heterogeneity`로 수정하였다.
   - DOCX 표지·제출서 생성 스크립트의 하드코딩 제목도 같은 기준으로 수정하고, 짧아진 국문 제목은 한 줄로 표시되도록 조정하였다.
   - 주요어와 영문 Keywords에서 도구명 중심의 `XGBoost`, `SHAP`, `XAI`, `Hedonic Price Model`을 제거하고, 원본 comment range인 `권역×연도 이질성`은 `시공간 이질성` / `spatiotemporal heterogeneity`로 교체하였다.

2. 인용 표기
   - `Lundberg와 Lee`, `Choy와 Ho`, `Kim, Choi와 Lee`, `Tarasov와 Dessoulavy-Śliwiński` 표기를 각각 `Lundberg & Lee`, `Choy & Ho`, `Kim, Choi & Lee`, `Tarasov & Dessoulavy-Śliwiński`로 정리하였다.
   - `et al.(연도)` 표기는 `et al. (연도)` 형식으로 본문 전체에서 정리하였다.

3. 근거 문헌 보강
   - 헤도닉 가격모형 한계 설명은 기존 본문에 있던 `Čeh et al. (2018)`, `Limsombunchai (2004)` 인용 맥락을 유지하였다.
   - 도로 우회 보정계수 `1.35`는 문헌 상수로 단정하지 않고, circuity 개념의 근거로 Boeing (2019)을 추가하였다.
   - Boeing (2019) PDF는 `references/papers/Boeing_2019_Circuity_Walkable_Drivable.pdf`에 저장하였다.

4. 주석의 본문 편입
   - 공간 정합, 시간 정합, 희소 시설 보정 설명을 `주 1`, `주 2`, `주 3` 형식에서 본문 설명으로 전환하였다.
   - Haversine 거리는 직선거리이며 실제 보행 네트워크 거리가 아니라는 한계를 본문에 유지하였다.

5. 설정값 표기
   - `n_estimators`, `max_depth`, `min_samples_leaf`, `learning_rate`, `subsample`, `colsample_bytree`, `tree_method` 같은 구현 변수명을 본문 서술에서 제거하였다.
   - `트리의 수`, `최대 깊이`, `최소 리프 노드 표본 수`, `학습률`, `행/열 단위 서브샘플링 비율` 등 원 용어 중심으로 바꾸었다.
   - 랜덤 포레스트와 XGBoost의 설정값 설명은 코드식 기호나 쉼표식 나열이 아니라, 탐색 절차와 최종 설정을 설명하는 문장으로 재작성하였다.
   - 랜덤 포레스트 설명 문단의 불필요한 영어 병기와 `하이퍼파라미터` 표현도 함께 정리하였다.

6. 논문 문체
   - `전체 수준 해석:`, `결정계수 (R²):`, `평균 제곱근 오차 (RMSE):` 등 콜론형 정의 문장을 일반 논문 문장으로 재작성하였다.
   - XGBoost 특징 및 XAI 필요성 설명의 나열식 문장도 같은 기준으로 정리하였다.

7. 표 설명 및 소제목
   - 소제목에서 `(<표 4-x>)` 형식을 제거하였다.
   - <표 4-1>, <표 4-2>, <표 4-7>, <표 4-10>, <표 4-11>, <표 4-12>, <표 4-13>, <표 4-14>, <표 4-15> 앞에 표의 목적 또는 해석 문단을 보강하였다.
   - 특히 <표 4-7>은 표 앞 목적 문단뿐 아니라 표 뒤 결과 해석 문단을 추가하였다.
   - `두 권역 구조 비교 - 핵심 해석`은 `권역별 예측 기여 구조의 종합`으로 바꾸고, 발견을 문장형 해석으로 재구성하였다.

8. 축약본 동기화
   - `paper/석사학위논문_박현근_compact.md`에 과거 피드백 전 표현이 남아 있어, 메인 최종 원고 `paper/석사학위논문_박현근.md`와 완전히 동기화하였다.
   - compact DOCX/PDF도 동기화된 원고 기준으로 재생성하였다.

9. 최종 편집 검수 보정
   - Microsoft Word 화면에서 <표 4-8> 앞 설명 문단이 페이지 하단과 다음 페이지로 문장 중간 분리되는 문제를 확인하였다.
   - 해당 설명 문단을 더 짧게 압축하고 불필요한 표 앞 구분선을 제거하여, 문단은 한 페이지 안에서 끝나고 표 4-8은 다음 페이지 상단에서 시작하도록 정리하였다.
   - 표지와 제출서의 영문 제목 줄바꿈도 Word 화면에서 재확인하였다. `Sale Price`와 `Structure in Seoul`이 분리되던 줄바꿈을 없애고, 제목 본문과 부제를 의미 단위 3줄로 정리하였다.
   - 표지와 제출서의 영문 제목 3줄을 모두 KoPubWorldBatang_Pro 12pt bold로 통일하였다.

10. 목차 페이지 번호 보정
   - DOCX 화면에서 목차 페이지 번호가 필드 캐시 때문에 모두 같은 값으로 보일 수 있는 문제를 확인하였다.
   - export 스크립트가 목차, 표 목차, 그림 목차의 `PAGEREF` 필드를 PDF 렌더 결과의 실제 페이지 번호 텍스트로 고정하도록 수정하였다.
   - 메인 및 compact DOCX 모두 목차 번호 55개를 실제 텍스트로 고정하고, DOCX 내부에 `PAGEREF` 필드가 남지 않도록 재생성하였다.

## 산출물

- 원문: `paper/석사학위논문_박현근.md`
- 축약 원문: `paper/석사학위논문_박현근_compact.md`
- DOCX: `paper/석사학위논문_박현근.docx`
- PDF: `paper/석사학위논문_박현근.pdf`
- 축약 DOCX/PDF: `paper/석사학위논문_박현근_compact.docx`, `paper/석사학위논문_박현근_compact.pdf`
- 추가 원문 PDF: `references/papers/Boeing_2019_Circuity_Walkable_Drivable.pdf`

## 검증

- DOCX/PDF export: `THESIS_EXPORT_PDF=1 .venv/bin/python scripts/export_docx_v8.py`
- compact DOCX/PDF export: `THESIS_EXPORT_PDF=1 .venv/bin/python scripts/export_docx_v8_compact.py`
- DOCX render: `.venv/bin/python .../render_docx.py paper/석사학위논문_박현근.docx --output_dir paper/render_check_20260603_advisor_feedback_final --emit_pdf`
- 렌더 결과: 86개 페이지 PNG와 PDF 생성
- 표지·제출서 확인: PDF 텍스트와 렌더 이미지에서 `서울시 아파트 매매가격 구조 분석`, `An Analysis of Apartment Sale Price Structure in Seoul` 반영 확인
- Word 확인: Microsoft Word에서 DOCX를 열어 표지와 제출서의 국문 제목이 한 줄로 표시되는 것을 확인하고, Word 찾기에서 `Unit-Area`, `GroupKFold`, `n_estimators`, `서울시 아파트 단위면적당` 검색 0건을 확인
- 최종 편집 검수: `paper/render_check_20260603_word_layout_final4`에 최종 PDF 기준 85페이지 PNG와 6개 접촉시트를 생성하고 전체를 확인하였다. `layout_metrics.json` 기준 빈 페이지 후보는 없었다.
- Word 재확인: 최종 DOCX를 저장하지 않은 이전 Word 세션에서 닫고 다시 열어 필드를 업데이트하였다. Word 상태 표시줄은 101페이지로 계산되며, 표 4-8 앞 수정 문구 검색 결과 설명 문단의 문장 중간 페이지 분리가 해소되었음을 확인하였다.
- 영문 제목 재확인: `paper/render_check_20260604_title_fix`에 표지·제출서·인준서 렌더 이미지를 생성하고, Word에서 최종 DOCX를 다시 열어 필드 업데이트 후 표지 영문 제목 표시를 확인하였다. DOCX XML 기준 표지와 제출서 영문 제목은 모두 12pt이며, 각 3줄이 동일한 글꼴·크기·굵기로 들어갔다.
- 잔여 패턴 검사: 메인/축약 원고, export 스크립트, DOCX XML, PDF 텍스트에서 2026-06-03 피드백 잔여 패턴 없음. 특히 주요어의 `권역×연도 이질성`과 Keywords의 `region×year heterogeneity`는 제거됨.
- 2026-06-04 재감사: 원본 Word comment range 기준 ID 6의 지적 대상이 `권역×연도 이질성`임을 재확인하였다. 해당 표현이 최종 주요어에 남아 있던 누락을 수정하고, 메인/compact DOCX와 PDF를 다시 생성하였다.
- 2026-06-04 원문-메모 교차분석: `paper/지도교수_피드백_원문교차분석_20260604.md`를 새로 작성하였다. 해당 파일은 원본 Word 메모의 표시 원문, 메모 원문, 의도 판정, 반영 방향을 ID 순서로 대조한다.
- 2026-06-04 목차 번호 재검증: 메인/compact DOCX의 `PAGEREF` 필드 잔여 수가 0개임을 확인하였다. 목차 첫 항목들은 `제1장 서론 1`, `제2절 연구의 범위 5`, `제2장 이론적 배경 및 선행연구 9`처럼 서로 다른 실제 페이지 번호 텍스트로 고정되어 있다.

## 남은 위험

- `1.35`는 서울 보행 네트워크에서 실측한 값이 아니므로, 본문에서는 Boeing (2019)을 circuity 개념 근거로만 사용하고 해당 값 자체는 휴리스틱 근사값으로 제한하였다.
- 목차 페이지 번호는 필드가 아니라 실제 텍스트로 고정되어 있으므로 Word에서 별도 필드 업데이트가 필요하지 않다.
