# 지도교수 피드백 원문-메모 교차분석 (2026-06-04)

원본 파일: `paper/석사학위논문_박현근_의견0603.docx`

추출 방식: DOCX 내부의 `word/comments.xml`에서 메모 원문을 추출하고, `word/document.xml`의 `commentRangeStart` / `commentRangeEnd` 범위와 대조하여 메모가 표시한 원문을 재구성하였다.

판정 원칙: 메모가 직접 표시한 원문만 기계적으로 바꾸지 않고, 같은 문체·표기 문제가 반복되는 본문 범위까지 확장 적용한다. 표 번호는 소제목에 넣지 않고 표 캡션과 본문 설명에서만 다룬다. 구현 변수명은 논문 본문에서 코드 기호로 노출하지 않고, 필요한 경우 원 용어를 문장 속에서 설명한다.

| ID | 표기된 원문 | 메모 원문 | 의도 판정 | 반영 방향 |
|---:|---|---|---|---|
| 0 | `단위면적당` | 제목에서는 이 표현이 불필요하지 않을까요? | 제목에서 종속변수 산식 수준의 세부 표현을 빼고 연구 대상 중심으로 정리 | 국문 제목을 `서울시 아파트 매매가격 구조 분석: 거리 기반 접근성과 시공간 이질성을 중심으로`로 수정 |
| 1 | `Unit-Area` | 국문제목이 수정되면 이 단어도 삭제 필요 | 영문 제목도 국문 제목과 동일한 축약 방향 적용 | 영문 제목에서 `Unit-Area` 삭제 |
| 6 | `권역×연도 이질성` | 키워드로 적절하지 않은 표현 | 주요어는 방법 조합 표현보다 핵심 개념 중심으로 정리 | 주요어/Keywords에서 해당 표현을 `시공간 이질성` / `spatiotemporal heterogeneity`로 수정 |
| 9 | `Lundberg와 Lee` | Lundberg & Lee (2017)로 표현하는 것이 적절함 | 외국 2인 저자 표기를 `&`로 통일 | `Lundberg & Lee (2017)`로 수정 |
| 12 | `일곱` | 일곱개의 | 관형 표현을 자연스러운 한국어로 수정 | `일곱 개의 절`로 수정 |
| 16 | `헤도닉 가격모형의 한계` | 아래의 주장에 대한 인용문헌이 필요할 듯 | 한계 주장에 문헌 근거가 필요 | 해당 절의 한계 설명에 `Čeh et al. (2018)`, `Limsombunchai (2004)` 인용 맥락 유지 |
| 18 | `Choy와 Ho` | Choy & Ho로 적어야y | 외국 2인 저자 표기 통일 | `Choy & Ho`로 수정 |
| 23 | `al.(2018)` | … al. (2018) -> 띄어쓰기.. 논문 전체적으로 수정 필요 | `et al. (연도)` 띄어쓰기 전체 정리 | 본문 전체의 `et al.(연도)` 패턴 제거 |
| 24 | `Choy와 Ho` | Choy & Ho (2023) -> 다른 부분도 이렇게 수정 필요 | 같은 저자 표기 규칙을 본문 전체에 적용 | `Choy & Ho (2023)`로 통일 |
| 25 | `와` | & | 외국 3인 저자 마지막 연결도 `&` 사용 | `Kim, Choi & Lee (2025)`로 수정 |
| 30 | `주 1`, `주 2`, `주 3` 설명 | 이 정도로 자세한 설명의 주석은 본문에서 설명하는 것이 적절함 | 긴 주석을 표 아래 주석이 아니라 본문 설명으로 편입 | 공간 정합, 시간 정합, 희소 시설 보정 설명을 본문 문단으로 이동 |
| 31 | `1.35` | 이 보정계수에 대한 근거(혹은 문헌)을 찾을 수 있을까? | 보정계수를 문헌 상수처럼 단정하지 말고 근거와 한계를 명시 | Boeing (2019)을 circuity 개념 근거로 추가하고, `1.35`는 휴리스틱 환산값으로 제한 |
| 32 | `GroupKFold` | 이렇게 특수한 전문용어를 사용한다면 이에 대한 근거(출처)나 개념을 보다 명확하게 설명해줘야함. | 구현명 대신 검증 설계의 의미를 설명 | `GroupKFold` 노출을 제거하고 동일 단지 반복 거래가 학습·평가에 동시에 들어가지 않도록 하는 그룹 분할로 설명 |
| 34 | `n_estimators` | 필요한 용어인지?? 트리의 수에 대한 기호에 불과하므로 불필요함. 만약 본문에서 지속적으로 이렇게 기호로 사용하려면 기호의 정의를 정리한 표를 제시하고 그 이후부터 기호를 사용해도 무방함 | 코드 변수명은 본문에 불필요. 지속 사용할 기호가 아니므로 제거 | `n_estimators` 제거, `트리의 수`로 문장화 |
| 35 | `max_depth` | 필요한 용어인지?? | 코드 변수명 제거 | `max_depth` 제거, `최대 깊이`로 문장화 |
| 36 | `min_samples_leaf` | 필요한 용어인지?? | 코드 변수명 제거 | `min_samples_leaf` 제거, `최소 리프 노드 표본 수`로 문장화 |
| 37 | `max_depth` 등 설정식 | 기호로 표현하지 말고 원 용어인 ‘최대 깊이' 사용 권장함.. 뒤의 기호도 마찬가지.. | 수식·코드식 설정 나열을 원 용어와 문장으로 재구성 | `max_depth ∈ {10, 15}` 같은 표현을 삭제하고, 최대 깊이·최소 리프 노드 표본 수·트리 수를 문장으로 설명 |
| 38 | `전체 수준 해석` | 논문에서는 이런 식으로 표현하지 않음. 문장으로 재구성해야 함. 아래도 마찬가지... | 콜론형 항목 제목을 논문 문장으로 재구성 | SHAP 설명을 `전체 수준 해석:` 형식이 아닌 문장형 설명으로 수정 |
| 39 | `결정계수 (R²):` | 나열식이 아니라 문장으로 표현해야 함. 아래도 마찬가지.. | 평가 지표를 사전식 나열이 아니라 문장으로 설명 | R², RMSE, MAE 설명을 문장형으로 수정 |
| 50 | `성능 비교` | 표만 제시되어 있고, 설명이 없음. 설명 추가 필요!! | 표 제시 전후에 목적과 결과 해석이 필요 | <표 4-7> 앞에 목적 문단, 표 뒤에 결과 해석 문단 추가 |
| 51 | `(<표 4-7>)` | 삭제 | 소제목에 표 번호를 넣지 말 것 | 소제목을 표 번호 없는 `공간·시간 정합 효과`로 수정 |
| 63 | `(<표 4-9>)` | 삭제 | 소제목에서 표 번호 삭제 | `권역별 예측 성능`으로 수정 |
| 65 | `SHAP 상위 5개 변수의 권역별 차이` | 설명추가 필요 | 표 4-10의 비교 의미를 본문에서 설명 | 권역별 상위 SHAP 변수 차이를 표 앞 문장으로 설명 |
| 66 | `(<표 4-10>)` | 삭제 | 소제목에서 표 번호 삭제 | `권역별 상위 SHAP 변수 차이`로 수정 |
| 70 | `(<표 4-11>)` | 삭제 | 소제목에서 표 번호 삭제 | `연도별 예측 성능`으로 수정 |
| 72 | `(<표 4-12>)` | 삭제 | 소제목에서 표 번호 삭제 | `연도별 상위 SHAP 변수`로 수정 |
| 75 | `(<표 4-13>)` | 제목에 표번호를 넣는 것은 부적절하므로 다른 부분에서도 수정 필요 | 표 번호 포함 소제목을 본문 전체에서 제거 | 본문 소제목의 `(<표 4-x>)` 패턴 전체 제거 |
| 81 | `— 핵심 해석` | ??? 굳이 있어야 하나? | 불필요한 강조형 소제목 삭제 또는 학술적 종합 제목으로 대체 | `권역별 예측 기여 구조의 종합`으로 수정 |

## 2026-06-04 추가 정정

- ID 34-37은 단순히 `n_estimators`, `max_depth`, `min_samples_leaf`만 제거하는 문제가 아니라, 불필요한 영어 병기와 코드식 설정값 나열을 논문 문장으로 바꾸라는 지적으로 재판정하였다.
- 이에 따라 랜덤 포레스트 문단의 `배깅 (bagging)`, `부트스트랩 (bootstrap)`, `분할 (split)`, `편향 (bias)`, `분산 (variance)`, `하이퍼파라미터` 표현을 정리하고, 설정값 설명을 문장형으로 재작성하였다.
- XGBoost 설정 설명도 `트리 수 600개, 최대 깊이 8, 학습률 0.05...`처럼 쉼표로 나열하던 문장을 `트리를 600개 생성하고 최대 깊이를 8로 제한했으며...` 형식의 문장으로 재구성하였다.
- ID 50-51과 ID 75는 표 번호를 제목에 넣지 말라는 피드백으로 재판정하였다. `성능 비교 (<표 4-7>)` 계열 제목은 표 번호 없는 분석 제목으로 바꾸고, 표 번호는 표 캡션과 본문 설명에서만 사용하도록 정리하였다.

## 최종 검증 결과

- 원본 Word 메모 수는 28개이며, ID는 `0, 1, 6, 9, 12, 16, 18, 23, 24, 25, 30, 31, 32, 34, 35, 36, 37, 38, 39, 50, 51, 63, 65, 66, 70, 72, 75, 81`이다.
- 메인 원고와 compact 원고에서 다음 잔여 패턴을 검색한 결과 0건이었다: `Unit-Area`, `권역×연도 이질성`, `region×year heterogeneity`, `Lundberg와`, `Choy와`, `Kim, Choi와`, `Tarasov와`, `et al.(연도)`, `주 1`, `주 2`, `주 3`, `GroupKFold`, `n_estimators`, `max_depth`, `min_samples_leaf`, `learning_rate`, `subsample`, `colsample_bytree`, `tree_method`, `전체 수준 해석`, `결정계수 (R²):`, `(<표 4-`, `— 핵심 해석`, `하이퍼파라미터`, `bagging`, `bootstrap`, `편향 (bias)`, `분산 (variance)`.
- 메인 DOCX와 compact DOCX의 내부 XML에서도 위 잔여 패턴은 0건이었다.
- 메인 PDF와 compact PDF 텍스트에서도 위 잔여 패턴은 0건이었다.
- 메인/compact DOCX의 목차 `PAGEREF` 필드 잔여 수는 0개이며, 목차 페이지 번호는 실제 텍스트로 고정되어 있다.
- 따라서 지도교수 메모 반영 여부를 기준으로 한 추가 메모별 재점검은 현재 산출물 기준으로 필요하지 않다. 다만 Word 화면에서의 최종 육안 편집 검수는 메모 반영 검증과 별개의 제출 전 편집 확인 절차이다.
